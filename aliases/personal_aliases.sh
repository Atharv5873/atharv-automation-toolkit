# ================================================================
#  Atharv's Personal Aliases and PATH Enhancements
#  Description : Custom shortcuts and helper configurations
# ================================================================

# --- Aliases ---
alias c="clear"
alias now="date +%F\ %T"
alias root="sudo su"
alias update="sudo apt update && sudo apt dist-upgrade -y && sudo apt clean"

# --- PATH Additions ---
export PATH="$PATH:$HOME/scripts"
